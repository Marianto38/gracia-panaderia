 <% if cart and not cart.line_items.empty? %>
  <div id="cart" class="bg-transparent rounded p-2">
  <%= render cart %>
  </div>
<% else %>
  <div id="cart">
    <p>tu carrito está vacio</p>
  </div>
<% end %>
